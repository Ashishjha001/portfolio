---
---
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>Ashish Jha | Business Consultant</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
	</head>
	<body>
		<nav>
			<div class="logo">Ashish Jha</div>
			<div class="nav-links">
				<a href="#work">Work</a>
				<a href="#about">About</a>
				<a href="#contact">Contact</a>
			</div>
		</nav>

		<section class="hero">
			<div class="grid-overlay"></div>
			<div class="blob blob-1"></div>
			<div class="blob blob-2"></div>
			<div class="blob blob-3"></div>
			<div class="spotlight" id="spotlight"></div>

			<div class="hero-content">
				<p class="eyebrow reveal-load">Independent Business Consultant</p>
				<h1 class="reveal-load" style="animation-delay:0.1s">Ashish Jha</h1>
				<p class="subtitle reveal-load" style="animation-delay:0.2s">
					Helping businesses turn data into decisions, analytics, and execution.
				</p>
				<a href="#work" class="cta magnetic reveal-load" style="animation-delay:0.3s">View Case Studies →</a>
			</div>

			<div class="scroll-indicator"><span></span></div>
		</section>

		<section id="work" class="work">
			<h2 class="reveal">Case Studies</h2>
			<p class="section-sub reveal">Monthly deep-dives into real business problems.</p>

			<div class="case-list">
				<div class="case-row" data-index="01">
					<div class="case-row-number">01</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Retail demand forecasting</h3>
						<p>How a mid-size retailer cut stockouts by rebuilding its demand planning process around a simple, explainable forecasting model.</p>
						<div class="case-row-meta">
							<span>Retail</span>
							<span>Forecasting</span>
							<span>Operations</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>

				<div class="case-row" data-index="02">
					<div class="case-row-number">02</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Customer churn diagnosis</h3>
						<p>Unpacking why a subscription business was losing customers in month two, and the three interventions that moved retention.</p>
						<div class="case-row-meta">
							<span>SaaS</span>
							<span>Churn</span>
							<span>Analytics</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>

				<div class="case-row" data-index="03">
					<div class="case-row-number">03</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Pricing strategy overhaul</h3>
						<p>Moving a B2B services company from flat pricing to a tiered model, and the revenue lift that followed within two quarters.</p>
						<div class="case-row-meta">
							<span>B2B</span>
							<span>Pricing</span>
							<span>Strategy</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>

				<div class="case-row" data-index="04">
					<div class="case-row-number">04</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Operational bottleneck audit</h3>
						<p>Mapping a manufacturing client's full production line to find the one step quietly costing them 18% of throughput.</p>
						<div class="case-row-meta">
							<span>Manufacturing</span>
							<span>Ops</span>
							<span>Process</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>

				<div class="case-row" data-index="05">
					<div class="case-row-number">05</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Customer segmentation rebuild</h3>
						<p>Replacing a stale, five-year-old segmentation model with one built on actual behavioral data — and what changed in targeting.</p>
						<div class="case-row-meta">
							<span>Marketing</span>
							<span>Segmentation</span>
							<span>Data</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>

				<div class="case-row" data-index="06">
					<div class="case-row-number">06</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Go-to-market for a new product</h3>
						<p>The demand-generation playbook used to take an early-stage product from zero to its first 100 paying customers.</p>
						<div class="case-row-meta">
							<span>GTM</span>
							<span>Growth</span>
							<span>Strategy</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>

				<div class="case-row" data-index="07">
					<div class="case-row-number">07</div>
					<div class="case-row-body">
						<span class="case-tag">Coming Soon</span>
						<h3>Data infrastructure cleanup</h3>
						<p>Untangling years of disconnected spreadsheets into a single source of truth the leadership team could actually trust.</p>
						<div class="case-row-meta">
							<span>Data</span>
							<span>Infrastructure</span>
							<span>Analytics</span>
						</div>
					</div>
					<div class="case-row-arrow">→</div>
				</div>
			</div>
		</section>

		<section id="about" class="about">
			<h2 class="reveal">About</h2>
			<p class="reveal">
			
I’m an NIT Surat Alumni, working at the intersection of business strategy, data, and technology.
I help businesses transform complex data into actionable insights and measurable outcomes identifying growth opportunities, generating demand, improving customer understanding, and optimizing operations.
My approach combines business understanding with advanced analytics and ML to move beyond insights and help businesses make better decisions, execute effectively, and grow.

			</p>
		</section>

		<section id="contact" class="contact">
	<h2 class="reveal">Let's Work Together</h2>
	<p class="reveal">Tell me a bit about what you need — I'll get back to you within 24 hours.</p>

	<form id="contact-form" class="contact-form reveal">
		<input type="hidden" name="access_key" value="0f76b840-061b-4f83-b0d8-7212eb4897e6" />
		<input type="hidden" name="subject" value="New Portfolio Inquiry - Ashish Jha" />
		<input type="checkbox" name="botcheck" style="display: none;" />

		<div class="form-row">
			<div class="form-group">
				<label for="name">Name *</label>
				<input type="text" id="name" name="name" required />
			</div>
			<div class="form-group">
				<label for="company">Company <span class="optional">(optional)</span></label>
				<input type="text" id="company" name="company" />
			</div>
		</div>

		<div class="form-row">
			<div class="form-group">
				<label for="email">Email *</label>
				<input type="email" id="email" name="email" required />
			</div>
			<div class="form-group">
				<label for="phone">Phone <span class="optional">(optional)</span></label>
				<input type="tel" id="phone" name="phone" />
			</div>
		</div>

		<div class="form-group full-width">
			<label for="message">What are you looking for? *</label>
			<textarea id="message" name="message" rows="4" required></textarea>
		</div>

		<button type="submit" class="cta magnetic submit-btn">Send Message →</button>
		<p id="form-status" class="form-status"></p>
	</form>
</section>

		<footer>
			<p>© 2026 Ashish Jha. All rights reserved.</p>
		</footer>
	</body>
</html>

<script>
	// Cursor spotlight glow inside hero
	const hero = document.querySelector('.hero');
	const spotlight = document.getElementById('spotlight');
	if (hero && spotlight) {
		hero.addEventListener('mousemove', (e) => {
			const rect = hero.getBoundingClientRect();
			spotlight.style.left = (e.clientX - rect.left) + 'px';
			spotlight.style.top = (e.clientY - rect.top) + 'px';
		});
	}

	// Magnetic buttons
	document.querySelectorAll('.magnetic').forEach((btn) => {
		btn.addEventListener('mousemove', (e) => {
			const rect = btn.getBoundingClientRect();
			const x = e.clientX - rect.left - rect.width / 2;
			const y = e.clientY - rect.top - rect.height / 2;
			btn.style.transform = `translate(${x * 0.25}px, ${y * 0.25}px)`;
		});
		btn.addEventListener('mouseleave', () => {
			btn.style.transform = 'translate(0,0)';
		});
	});

	// Sequential slide-in reveal for case study rows (McKinsey/BCG-style stagger)
	const caseRows = document.querySelectorAll('.case-row');
	const caseObserver = new IntersectionObserver(
		(entries) => {
			entries.forEach((entry) => {
				if (entry.isIntersecting) {
					const rows = Array.from(document.querySelectorAll('.case-row'));
					const idx = rows.indexOf(entry.target);
					entry.target.style.transitionDelay = `${idx * 0.12}s`;
					entry.target.classList.add('visible');
					caseObserver.unobserve(entry.target);
				}
			});
		},
		{ threshold: 0.15, rootMargin: '0px 0px -60px 0px' }
	);
	caseRows.forEach((row) => caseObserver.observe(row));

	// Scroll reveal animations
	const revealEls = document.querySelectorAll('.reveal');
	const observer = new IntersectionObserver(
		(entries) => {
			entries.forEach((entry) => {
				if (entry.isIntersecting) {
					entry.target.classList.add('visible');
					observer.unobserve(entry.target);
				}
			});
		},
		{ threshold: 0.15 }
	);
	revealEls.forEach((el) => observer.observe(el));
	
	// Contact form submission
	const form = document.getElementById('contact-form');
	const status = document.getElementById('form-status');
	if (form) {
		form.addEventListener('submit', async (e) => {
			e.preventDefault();
			const formData = new FormData(form);
			const submitBtn = form.querySelector('.submit-btn');
			submitBtn.textContent = 'Sending...';
			submitBtn.disabled = true;

			try {
				const res = await fetch('https://api.web3forms.com/submit', {
					method: 'POST',
					body: formData,
				});
				const result = await res.json();
				if (result.success) {
					status.textContent = "Thanks! I'll get back to you soon.";
					status.style.color = '#16a34a';
					form.reset();
				} else {
					status.textContent = 'Something went wrong. Please try again.';
					status.style.color = '#dc2626';
				}
			} catch (err) {
				status.textContent = 'Something went wrong. Please try again.';
				status.style.color = '#dc2626';
			}

			submitBtn.textContent = 'Send Message →';
			submitBtn.disabled = false;
		});
	}
</script>

<style is:global>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	body {
		font-family: 'Inter', 'Segoe UI', sans-serif;
		background: #ffffff;
		color: #0f172a;
		line-height: 1.6;
	}

	nav {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 24px 60px;
		background: rgba(255, 255, 255, 0.85);
		backdrop-filter: blur(10px);
		z-index: 100;
		border-bottom: 1px solid #e2e8f0;
	}

	.logo {
		font-weight: 700;
		font-size: 1.1rem;
		color: #1e3a8a;
	}

	.nav-links a {
		position: relative;
		margin-left: 32px;
		text-decoration: none;
		color: #334155;
		font-weight: 500;
		transition: color 0.2s;
	}

	.nav-links a::after {
		content: '';
		position: absolute;
		left: 0;
		bottom: -4px;
		width: 0;
		height: 2px;
		background: #2563eb;
		transition: width 0.25s ease;
	}

	.nav-links a:hover {
		color: #2563eb;
	}

	.nav-links a:hover::after {
		width: 100%;
	}

	.hero {
		position: relative;
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		overflow: hidden;
		padding: 0 24px;
		background: linear-gradient(180deg, #f8fafc 0%, #eff6ff 100%);
	}

	.grid-overlay {
		position: absolute;
		inset: 0;
		background-image: linear-gradient(rgba(30, 58, 138, 0.05) 1px, transparent 1px),
			linear-gradient(90deg, rgba(30, 58, 138, 0.05) 1px, transparent 1px);
		background-size: 48px 48px;
		z-index: 0;
	}

	.blob {
		position: absolute;
		border-radius: 50%;
		filter: blur(70px);
		opacity: 0.5;
		z-index: 0;
	}

	.blob-1 {
		width: 480px;
		height: 480px;
		background: radial-gradient(circle at 30% 30%, #60a5fa, #2563eb);
		top: -120px;
		left: -100px;
		animation: float1 18s ease-in-out infinite;
	}

	.blob-2 {
		width: 400px;
		height: 400px;
		background: radial-gradient(circle at 70% 70%, #93c5fd, #1e3a8a);
		bottom: -140px;
		right: -100px;
		animation: float2 22s ease-in-out infinite;
	}

	.blob-3 {
		width: 280px;
		height: 280px;
		background: radial-gradient(circle, #bfdbfe, #3b82f6);
		top: 45%;
		left: 65%;
		animation: float3 20s ease-in-out infinite;
	}

	@keyframes float1 {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(60px, 40px) scale(1.1); }
	}
	@keyframes float2 {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(-50px, -30px) scale(1.05); }
	}
	@keyframes float3 {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(-40px, 50px) scale(0.95); }
	}

	.spotlight {
		position: absolute;
		width: 500px;
		height: 500px;
		border-radius: 50%;
		background: radial-gradient(circle, rgba(37, 99, 235, 0.18), transparent 70%);
		pointer-events: none;
		transform: translate(-50%, -50%);
		z-index: 1;
		opacity: 0;
		transition: opacity 0.3s;
	}

	.hero:hover .spotlight {
		opacity: 1;
	}

	.hero-content {
		position: relative;
		z-index: 2;
		text-align: center;
		max-width: 700px;
		pointer-events: none;
	}

	.hero-content .cta {
		pointer-events: auto;
	}

	@keyframes fadeInUp {
		from { opacity: 0; transform: translateY(24px); }
		to { opacity: 1; transform: translateY(0); }
	}

	.reveal-load {
		opacity: 0;
		animation: fadeInUp 0.8s ease forwards;
	}

	.eyebrow {
		color: #2563eb;
		font-weight: 600;
		letter-spacing: 0.05em;
		text-transform: uppercase;
		font-size: 0.85rem;
		margin-bottom: 16px;
	}

	.hero h1 {
		font-size: clamp(2.5rem, 6vw, 4.5rem);
		font-weight: 800;
		color: #0f172a;
		margin-bottom: 20px;
	}

	.subtitle {
		font-size: 1.2rem;
		color: #334155;
		margin-bottom: 36px;
	}

	.cta {
		display: inline-block;
		background: #1e3a8a;
		color: #fff;
		padding: 14px 32px;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 600;
		transition: background 0.2s, transform 0.2s ease-out;
	}

	.cta:hover {
		background: #2563eb;
	}

	.scroll-indicator {
		position: absolute;
		bottom: 32px;
		left: 50%;
		transform: translateX(-50%);
		z-index: 2;
	}

	.scroll-indicator span {
		display: block;
		width: 10px;
		height: 10px;
		border-right: 2px solid #2563eb;
		border-bottom: 2px solid #2563eb;
		transform: rotate(45deg);
		animation: bounce 2s infinite;
	}

	@keyframes bounce {
		0%, 100% { transform: rotate(45deg) translate(0, 0); opacity: 0.6; }
		50% { transform: rotate(45deg) translate(6px, 6px); opacity: 1; }
	}

	section {
		padding: 100px 60px;
		max-width: 1100px;
		margin: 0 auto;
	}

	.reveal {
		opacity: 0;
		transform: translateY(24px);
		transition: opacity 0.6s ease, transform 0.6s ease;
	}

	.reveal.visible {
		opacity: 1;
		transform: translateY(0);
	}

	h2 {
		font-size: 2.2rem;
		font-weight: 800;
		color: #0f172a;
		margin-bottom: 12px;
	}

	h2::before {
		content: '';
		display: block;
		width: 40px;
		height: 4px;
		background: #2563eb;
		border-radius: 2px;
		margin-bottom: 16px;
	}

	.section-sub {
		color: #64748b;
		margin-bottom: 48px;
	}

	/* ===== Work section — McKinsey/BCG-style sequential sliding rows ===== */

	.case-list {
		display: flex;
		flex-direction: column;
		border-top: 1px solid #e2e8f0;
	}

	.case-row {
		display: grid;
		grid-template-columns: 72px 1fr 32px;
		align-items: center;
		gap: 32px;
		padding: 36px 8px;
		border-bottom: 1px solid #e2e8f0;
		cursor: pointer;
		text-decoration: none;
		color: inherit;

		opacity: 0;
		transform: translateX(-40px);
		transition: opacity 0.6s ease, transform 0.6s ease,
			background 0.25s ease, border-color 0.25s ease, padding-left 0.25s ease;
	}

	.case-row.visible {
		opacity: 1;
		transform: translateX(0);
	}

	.case-row:hover {
		background: #f8fafc;
		border-color: #2563eb;
		padding-left: 20px;
	}

	.case-row-number {
		font-size: 1.4rem;
		font-weight: 800;
		color: #93c5fd;
		font-variant-numeric: tabular-nums;
		transition: color 0.25s ease;
	}

	.case-row:hover .case-row-number {
		color: #2563eb;
	}

	.case-row-body {
		min-width: 0;
	}

	.case-tag {
		display: inline-block;
		font-size: 0.7rem;
		font-weight: 700;
		letter-spacing: 0.06em;
		text-transform: uppercase;
		color: #2563eb;
		background: #eff6ff;
		padding: 4px 10px;
		border-radius: 999px;
		margin-bottom: 10px;
	}

	.case-row-body h3 {
		font-size: 1.4rem;
		font-weight: 700;
		color: #0f172a;
		margin-bottom: 8px;
	}

	.case-row-body p {
		color: #64748b;
		font-size: 1rem;
		max-width: 640px;
		margin-bottom: 14px;
	}

	.case-row-meta {
		display: flex;
		flex-wrap: wrap;
		gap: 10px;
	}

	.case-row-meta span {
		font-size: 0.78rem;
		font-weight: 600;
		color: #475569;
		background: #f1f5f9;
		padding: 4px 10px;
		border-radius: 6px;
	}

	.case-row-arrow {
		font-size: 1.3rem;
		color: #cbd5e1;
		transition: transform 0.25s ease, color 0.25s ease;
		justify-self: end;
	}

	.case-row:hover .case-row-arrow {
		transform: translateX(6px);
		color: #2563eb;
	}

	@media (max-width: 720px) {
		.case-row {
			grid-template-columns: 44px 1fr;
			gap: 16px;
			padding: 28px 4px;
		}
		.case-row-arrow {
			display: none;
		}
		.case-row-number {
			font-size: 1.1rem;
		}
		.case-row-body h3 {
			font-size: 1.15rem;
		}
	}

	/* ===== end Work section ===== */

	.about p {
		font-size: 1.15rem;
		color: #334155;
		max-width: 700px;
	}

	.contact {
		text-align: center;
		background: #f8fafc;
		border-radius: 16px;
		padding: 80px 40px;
	}

	.contact p {
		color: #64748b;
		margin-bottom: 28px;
	}

	footer {
		text-align: center;
		padding: 40px;
		color: #94a3b8;
		font-size: 0.9rem;
	}

	@media (max-width: 640px) {
		nav { padding: 20px; }
		.nav-links a { margin-left: 16px; font-size: 0.9rem; }
		section { padding: 60px 24px; }
	}
		.contact-form {
		max-width: 600px;
		margin: 0 auto;
		text-align: left;
	}

	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 20px;
		margin-bottom: 20px;
	}

	.form-group {
		display: flex;
		flex-direction: column;
	}

	.form-group.full-width {
		margin-bottom: 24px;
	}

	.form-group label {
		font-size: 0.9rem;
		font-weight: 600;
		color: #334155;
		margin-bottom: 6px;
	}

	.optional {
		font-weight: 400;
		color: #94a3b8;
	}

	.form-group input,
	.form-group textarea {
		padding: 12px 14px;
		border: 1px solid #cbd5e1;
		border-radius: 8px;
		font-family: inherit;
		font-size: 1rem;
		background: #fff;
		transition: border-color 0.2s, box-shadow 0.2s;
		resize: vertical;
	}

	.form-group input:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: #2563eb;
		box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.12);
	}

	.submit-btn {
		border: none;
		cursor: pointer;
		font-size: 1rem;
		width: 100%;
	}

	.form-status {
		margin-top: 16px;
		font-size: 0.95rem;
		text-align: center;
		min-height: 20px;
	}

	@media (max-width: 640px) {
		.form-row {
			grid-template-columns: 1fr;
			gap: 0;
		}
	}
</style>